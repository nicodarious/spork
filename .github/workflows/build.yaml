# first ci/cd yaml build script
name: goproxybuild
on: [push, workflow_dispatch ]

jobs:
  job_one:
    runs-on: ubuntu-minimal
    steps:
      - name: spork
        uses: actions/checkout@v2
      - name: run a thing
        run: |
          echo "PING! "
          echo "..."
          echo "..."
          echo "..."
          cpupower frequency-info
          echo "..."
          echo "..."
          echo "..."
          lscpu
          echo "..."
          echo "..."
          cat go-proxy/main.go










      # - name: grab spack
      #   users:
      #   with:
      #     repository:
      #     path:
        
      # - name:
      #   run: |